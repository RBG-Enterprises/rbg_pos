web: bin/rails server -p 3000
# --watch=forever: foreman closes child stdin, which makes esbuild's plain
# --watch exit immediately (and foreman then SIGTERMs everything).
js: yarn build --watch=forever
css: yarn build:css --watch
